/**
 * Shared CARTO API key used by applications that consume the map library.
 *
 * Note:
 * Browser-side API keys are not secrets. If the key must be protected,
 * it should not be embedded directly into frontend code.
 */
export const CARTO_API_KEY = '';
